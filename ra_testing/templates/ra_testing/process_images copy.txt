<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Image Viewer App Concept</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    
    <style>
        /* Base App Reset */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            background-color: #f4f6f9;
            overflow: hidden; /* Prevent body scroll, handle scrolling in components */
        }

        .app-shell {
            display: flex;
            flex-direction: column;
            height: 100vh;
        }

        /* 1. Header Area */
        .app-header {
            height: 60px;
            background-color: #ffffff;
            border-bottom: 1px solid #e0e0e0;
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0 20px;
            flex-shrink: 0;
        }

        /* 2. Main Workspace */
        .app-workspace {
            display: flex;
            flex: 1;
            overflow: hidden; /* Contains the children */
        }

        /* 2a. Canvas Area (Left) */
        .canvas-area {
            flex: 1;
            background-color: #1e1e1e; /* Dark background to make images pop */
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            padding: 20px;
        }

        .canvas-placeholder {
            width: 100%;
            height: 100%;
            background-color: #2a2a2a;
            border-radius: 8px;
            border: 2px dashed #444;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #888;
        }

        .frame-counter {
            position: absolute;
            top: 20px;
            left: 50%;
            transform: translateX(-50%);
            background: rgba(0, 0, 0, 0.7);
            color: #fff;
            padding: 6px 16px;
            border-radius: 20px;
            font-size: 0.85rem;
            z-index: 10;
        }

        .nav-btn {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            background: rgba(255,255,255,0.1);
            border: none;
            color: white;
            padding: 15px;
            border-radius: 50%;
            cursor: pointer;
            transition: background 0.2s;
        }

        .nav-btn:hover {
            background: rgba(255,255,255,0.3);
        }

        .nav-left { left: 20px; }
        .nav-right { right: 20px; }

        /* 2b. Sidebar Area (Right) */
        .sidebar-area {
            width: 320px;
            background-color: #ffffff;
            border-left: 1px solid #e0e0e0;
            display: flex;
            flex-direction: column;
            flex-shrink: 0;
        }

        .sidebar-section {
            padding: 15px;
            border-bottom: 1px solid #f0f0f0;
        }

        .json-viewer {
            flex: 1;
            overflow-y: auto;
            padding: 15px;
            background-color: #fafafa;
            font-family: monospace;
            font-size: 0.85rem;
        }

        /* 3. Bottom Action Bar */
        .app-footer {
            background-color: #ffffff;
            border-top: 1px solid #e0e0e0;
            padding: 10px 20px;
            flex-shrink: 0;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 15px;
            overflow-x: auto; /* Scroll horizontally if screen is too small */
        }

        .action-group {
            display: flex;
            gap: 8px;
            align-items: center;
            border-right: 1px solid #e0e0e0;
            padding-right: 15px;
        }
        
        .action-group:last-child {
            border-right: none;
        }

        .gpx-upload-form {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 0.85rem;
        }

        /* Mobile Responsiveness */
        @media (max-width: 992px) {
            .app-workspace {
                flex-direction: column;
            }
            .sidebar-area {
                width: 100%;
                height: 40vh; /* Takes bottom part of screen on mobile */
                border-left: none;
                border-top: 1px solid #e0e0e0;
            }
            .app-footer {
                flex-wrap: wrap;
                overflow-x: visible;
                height: auto;
                justify-content: center;
            }
            .action-group {
                border-right: none;
                padding-right: 0;
                justify-content: center;
                flex-wrap: wrap;
            }
        }
    </style>
</head>
<body>

    <div class="app-shell">
        
        <header class="app-header">
            <div class="d-flex align-items-center gap-3">
                <button class="btn btn-sm btn-outline-secondary">← Menu</button>
                <h5 class="m-0">Image Viewer</h5>
            </div>
            <div>
                <span class="badge bg-primary">Survey: 1024</span>
                <span class="badge bg-secondary">Road: 55A</span>
            </div>
        </header>

        <main class="app-workspace">
            
            <section class="canvas-area">
                <div class="frame-counter">Frame: 45 Out Of 120 Frames</div>
                
                <button class="nav-btn nav-left">❮</button>
                
                <div class="canvas-placeholder">
                    [ Canvas Image Area ]
                </div>

                <button class="nav-btn nav-right">❯</button>
            </section>

            <aside class="sidebar-area">
                <div class="sidebar-section">
                    <h6 class="mb-3 text-muted">Controller Settings</h6>
                    <div class="mb-2">
                        <label class="form-label" style="font-size: 0.8rem;">Select Label</label>
                        <select class="form-select form-select-sm">
                            <option>Pothole</option>
                            <option>Crack</option>
                        </select>
                    </div>
                    <div>
                        <label class="form-label" style="font-size: 0.8rem;">Resolution</label>
                        <select class="form-select form-select-sm" disabled>
                            <option>1280x1080</option>
                        </select>
                    </div>
                </div>

                <div class="sidebar-section">
                    <input type="text" class="form-control form-control-sm" placeholder="Search JSON key...">
                </div>

                <div class="json-viewer">
                    // JSON Container Data will populate here
                    <br><br>
                    {<br>
                    &nbsp;&nbsp;"timestamp": "2024-11-13 13:43:00",<br>
                    &nbsp;&nbsp;"anomalies": []<br>
                    }
                </div>
            </aside>
        </main>

        <footer class="app-footer">
            
            <div class="action-group">
                <button class="btn btn-sm btn-primary">Edit</button>
                <button class="btn btn-sm btn-secondary" disabled>Undo</button>
                <button class="btn btn-sm btn-danger">Patch Frame</button>
            </div>

            <div class="action-group">
                <button class="btn btn-sm btn-warning">DL Json</button>
                <button class="btn btn-sm btn-info text-white">DL Merged Json</button>
                <button class="btn btn-sm btn-success">DL Image</button>
            </div>

            <div class="action-group">
                <button class="btn btn-sm btn-outline-danger">Clear JSON</button>
            </div>

            <div class="action-group gpx-upload-form">
                <input type="file" class="form-control form-control-sm" style="width: 180px;">
                <div class="form-check m-0">
                    <input class="form-check-input" type="checkbox" id="tzCheck">
                    <label class="form-check-label" for="tzCheck">+5:30</label>
                </div>
                <button class="btn btn-sm btn-success">Upload GPX</button>
            </div>

        </footer>

    </div>

</body>
</html>